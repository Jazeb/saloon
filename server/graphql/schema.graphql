
type Subscription {
	NEW_JOB_ALERT: Job
}

type Subscription {
	LOCATION_UPDATE: Location
}

type Job {
	lat: String!
	long: String!
	service_id: Int!
	parent_id: Int
	# sub_service_id: Int
	# service_name: String!
}

type Location {
	lat: String!
	long: String!
	user_id: Int!
}

# this query is not used
type Query {
	GET_PRODUCT(product_id: Int!): [Job]
}